<h2>Comments</h2>

<% @comments.each do |comment| %>
    <p>
      <b>Commenter:</b>
      <%= User.find(comment.userid).username %>
    </p>

    <p>
      <b>Comment:</b>
      <%= comment.content %>
    </p>
<% end %>