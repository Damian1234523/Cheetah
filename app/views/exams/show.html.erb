<%= render '/breadcru' %>

<h1 style="margin-top: 0em"> <%= @exam.name %> </h1>
<li> <%=link_to("Create text", text_new_path(exam_id: @exam), {:class => "btn btn-default", style: "margin-down:10px"})%></li>

<table class="table table-hover">
  <thead>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Typ</th>
  </tr>
  </thead>
  <% @text.each do |txt| %>
      <tr class="table-row" data-href=<%= university_course_lecturer_exam_text_path(@un.id, @co.id, @le.id, @exam.id, txt.id) %>>
        <td><%= txt.title %></td>
        <td><%= txt.description %></td>
        <td>not yet implemented</td>
      </tr>
  <%end%>
</table>

